---
phase: 4
plan: 3
depends_on: [4.1, 4.2]
must_haves:
  - "AgentRegistry deployed to Sepolia with mesh name AgentMesh-Demo"
  - "MeshCreated event visible on Sepolia Etherscan"
  - "Contract source code verified on Sepolia Etherscan (green checkmark)"
  - "meshes.json at repo root contains live Sepolia contract address"
  - "registerAgent call succeeds on the live Sepolia contract"
---

<plan>
  <meta>
    <phase>4</phase>
    <plan>3</plan>
    <title>Sepolia Deploy and Verify</title>
    <depends-on>4.1,4.2</depends-on>
  </meta>
  <tasks>
    <task type="checkpoint:human-verify">
      <n>Fund deployer keystore wallet on Sepolia</n>
      <context>Foundry keystore wallet created in Plan 4.2 via cast wallet new deployer. Address is printed by that command and stored in the system keystore. Sepolia ETH is needed to pay gas for the AgentRegistry constructor transaction (approx 0.01-0.05 ETH). DEPLOYER_ACCOUNT env var holds the keystore account name.</context>
      <domain>External: Sepolia faucet to deployer EOA balance</domain>
      <files>None (wallet is on-chain state + keystore; docs/DEPLOYMENT.md section 2 for faucet URLs)</files>
      <test>
RED: Verify deployer has insufficient balance before funding.
Run: source .env and cast balance $(cast wallet address --account $DEPLOYER_ACCOUNT) --rpc-url $RPC_URL
Expected: 0 or near-0 ETH. If already funded, skip this task.
      </test>
      <action>
GREEN:
1. Get deployer address: cast wallet address --account $DEPLOYER_ACCOUNT
2. Fund from Sepolia faucets (need at least 0.05 ETH):
   - https://cloud.google.com/application/web3/faucet/ethereum/sepolia (0.05 ETH/day)
   - https://sepoliafaucet.com (0.5 ETH/day, requires Alchemy account)
   - https://sepolia-faucet.pk910.de (PoW miner, no cap)
3. Wait ~30 seconds, then confirm: cast balance $(cast wallet address --account $DEPLOYER_ACCOUNT) --rpc-url $RPC_URL
      </action>
      <refactor>None — faucet interactions leave no local artifacts.</refactor>
      <verify>cast balance $(cast wallet address --account $DEPLOYER_ACCOUNT) --rpc-url $RPC_URL
Output must show at least 10000000000000000 wei (0.01 ETH).</verify>
      <done>Deployer wallet has at least 0.01 ETH on Sepolia. The balance command returns a non-zero value sufficient to cover deploy gas.</done>
    </task>

    <task type="checkpoint:human-verify">
      <n>Deploy AgentRegistry to Sepolia and populate meshes.json</n>
      <context>Deploy.s.sol (created in Plan 4.1) reads MESH_NAME from env, deploys AgentRegistry(meshName), writes meshes.json to repo root via vm.writeJson, and emits a stdout summary. Foundry keystore prompts for wallet password at broadcast time. solc 0.8.30, optimizer 200 runs.</context>
      <domain>AgentRegistry contract (Registry bounded context); meshes.json (deployment record)</domain>
      <files>agentmesh-contracts/script/Deploy.s.sol (read), meshes.json (written by script)</files>
      <test>
RED: Confirm meshes.json does not yet contain a live Sepolia address.
Run: cat meshes.json 2>/dev/null || echo "not found"
Expected: file absent or contains only test/placeholder data.
      </test>
      <action>
GREEN: From repo root with .env loaded:
  source .env
  cd agentmesh-contracts
  forge script script/Deploy.s.sol:Deploy \
    --rpc-url $RPC_URL \
    --account $DEPLOYER_ACCOUNT \
    --sender $(cast wallet address --account $DEPLOYER_ACCOUNT) \
    --broadcast \
    --slow \
    -vvv
Enter keystore password when prompted.
Then confirm: cat ../meshes.json
      </action>
      <refactor>
1. Verify meshes.json contains valid JSON with address, name, deployed_at fields.
2. Note the deployed address for use in verification and smoke test tasks.
      </refactor>
      <verify>
1. cat meshes.json — contains valid JSON array with 0x address, mesh name "AgentMesh-Demo", and deployed_at block number
2. Open https://sepolia.etherscan.io/address/DEPLOYED_ADDRESS — contract creation tx visible, MeshCreated event in Logs tab
      </verify>
      <done>meshes.json contains a valid checksummed 0x address. Sepolia Etherscan shows the contract creation transaction and the MeshCreated event log with name="AgentMesh-Demo".</done>
    </task>

    <task type="checkpoint:human-verify">
      <n>Verify contract source code on Sepolia Etherscan</n>
      <context>Etherscan verification is a separate step from deployment (per project decision — more resilient to API flakiness). AgentRegistry imports IAgentRegistry. Constructor arg is string "AgentMesh-Demo". Compiler: solc 0.8.30, optimizer 200 runs. Chain ID 11155111.</context>
      <domain>AgentRegistry contract (verification metadata on Etherscan)</domain>
      <files>agentmesh-contracts/src/AgentRegistry.sol (source sent to Etherscan), agentmesh-contracts/src/interfaces/IAgentRegistry.sol</files>
      <test>
RED: Before verification, Etherscan shows bytecode only, no source code.
Open https://sepolia.etherscan.io/address/DEPLOYED_ADDRESS#code — should show "unable to locate matching source code".
      </test>
      <action>
GREEN: From agentmesh-contracts/ directory:
  source ../.env
  DEPLOYED_ADDRESS=$(python3 -c "import json; d=json.load(open('../meshes.json')); print(d[0]['address'])")

  forge verify-contract \
    $DEPLOYED_ADDRESS \
    src/AgentRegistry.sol:AgentRegistry \
    --chain 11155111 \
    --etherscan-api-key $ETHERSCAN_API_KEY \
    --compiler-version 0.8.30 \
    --num-of-optimizations 200 \
    --constructor-args $(cast abi-encode "constructor(string)" "AgentMesh-Demo") \
    --watch

Expected output: "Contract successfully verified."
      </action>
      <refactor>Update docs/DEPLOYMENT.md section 5 with the exact verified address and verification command used.</refactor>
      <verify>
Open https://sepolia.etherscan.io/address/DEPLOYED_ADDRESS#code
Expected: green checkmark, "Contract Source Code Verified", Solidity source visible with AgentRegistry and IAgentRegistry.
ABI tab shows all functions: registerAgent, getAgent, getAllAgents, searchByCapability, createTask, completeTask, releasePayment.
      </verify>
      <done>Etherscan shows green verified checkmark on the contract address. Source code is publicly readable. ABI is auto-populated with all public functions.</done>
    </task>

    <task type="checkpoint:human-verify">
      <n>Smoke test: register an agent on live Sepolia contract</n>
      <context>AgentRegistry.registerAgent takes (agentId string, capabilities string[], pricePerTask uint256, wsEndpoint string). The deployer wallet has residual Sepolia ETH from deploy. This validates the live contract accepts state-changing calls.</context>
      <domain>AgentRegistry contract (Registry bounded context — registerAgent path)</domain>
      <files>meshes.json (read for address)</files>
      <test>
RED: Confirm no agents registered yet.
  DEPLOYED_ADDRESS=$(python3 -c "import json; d=json.load(open('meshes.json')); print(d[0]['address'])")
  cast call $DEPLOYED_ADDRESS "getAllAgents()(address[])" --rpc-url $RPC_URL
Expected: empty array []
      </test>
      <action>
GREEN: Send a registerAgent transaction:
  source .env
  DEPLOYED_ADDRESS=$(python3 -c "import json; d=json.load(open('meshes.json')); print(d[0]['address'])")

  cast send $DEPLOYED_ADDRESS \
    "registerAgent(string,string[],uint256,string)" \
    "smoke-test-agent" \
    '["diagnostics","smoke-test"]' \
    1000000000000000 \
    "wss://example.com/agent/smoke-test" \
    --rpc-url $RPC_URL \
    --account $DEPLOYER_ACCOUNT

Then read back:
  cast call $DEPLOYED_ADDRESS \
    "getAgent(address)((string,string[],uint256,string,uint8,bool))" \
    $(cast wallet address --account $DEPLOYER_ACCOUNT) \
    --rpc-url $RPC_URL
      </action>
      <refactor>Document the smoke test result in docs/DEPLOYMENT.md with the transaction hash and Etherscan link.</refactor>
      <verify>
1. cast send returns transaction hash with status: 1 (success)
2. cast call getAgent returns tuple with agentId="smoke-test-agent", capabilities=["diagnostics","smoke-test"], reputation=50, active=true
3. AgentRegistered event visible on Sepolia Etherscan
      </verify>
      <done>getAllAgents() returns array with deployer address. getAgent(deployer) returns smoke-test agent struct with reputation=50 and active=true. AgentRegistered event visible on Etherscan.</done>
    </task>
  </tasks>
</plan>