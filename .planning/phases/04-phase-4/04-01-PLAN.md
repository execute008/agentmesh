---
phase: 4
plan: 1
depends_on: []
must_haves:
  - "agentmesh-contracts/script/Deploy.s.sol compiles with forge build (0 errors)"
  - "Deploy script reads MESH_NAME from env var and deploys AgentRegistry with that name"
  - "Deploy script writes meshes.json to repo root with address, name, deployed_at fields"
  - "Deploy script emits human-readable summary to stdout (address, mesh name, block, chain ID)"
  - "All 23 existing AgentRegistry tests still pass"
---

<plan>
  <meta>
    <phase>4</phase>
    <plan>1</plan>
    <title>Deploy Script</title>
    <depends-on></depends-on>
  </meta>
  <tasks>
    <task type="auto">
      <n>Write Deploy.s.sol — Foundry deploy script for AgentRegistry</n>
      <context>The agentmesh-contracts project uses Foundry with solc 0.8.30, optimizer enabled at 200 runs. forge-std is already available at lib/forge-std. The Script base contract lives at lib/forge-std/src/Script.sol; it transitively imports console, stdJson, and all VmSafe cheatcodes. The remapping forge-std/=lib/forge-std/src/ is the convention used in the existing test file. AgentRegistry constructor takes a single string _meshName and emits MeshCreated(_meshName, owner). No script/ directory exists yet. vm.envString(key) reverts if the env var is unset, which is the desired guard behaviour. stdJson serialize+write pattern: build object with vm.serializeX calls on a named key, then vm.writeJson(finalJson, path). The simplest correct approach is to build the JSON string manually via string.concat and call vm.writeJson(json, path).</context>
      <domain>AgentRegistry deployment, meshes.json artifact</domain>
      <files>agentmesh-contracts/script/Deploy.s.sol (new file)</files>
      <test>
RED — write agentmesh-contracts/test/DeployScript.t.sol before implementing the script:
1. test_deploy_setsCorrectMeshName: set env var MESH_NAME="TestDeploy", run new Deploy() and call deploy.run(), assert returned AgentRegistry.meshName() == "TestDeploy".
2. test_deploy_reverts_when_meshname_missing: vm.expectRevert() then call run() without setting MESH_NAME — expect vm.envString revert.
3. test_deploy_writesJsonFile: after run(), call vm.readFile("meshes.json"), parse with stdJson, assert address field is non-zero, name field equals "TestDeploy", deployed_at is > 0.
Run: cd agentmesh-contracts && forge test --match-contract DeployScript — all 3 should FAIL before the script exists.
      </test>
      <action>
GREEN — create agentmesh-contracts/script/Deploy.s.sol:
- Import {Script, console} from "forge-std/Script.sol" and {AgentRegistry} from "../src/AgentRegistry.sol"
- function run() external returns (AgentRegistry registry):
  1. Read meshName via vm.envString("MESH_NAME")
  2. vm.startBroadcast() / vm.stopBroadcast() wrapping: registry = new AgentRegistry(meshName)
  3. Build JSON: vm.serializeAddress("entry", "address", address(registry)), vm.serializeString("entry", "name", meshName), entry = vm.serializeUint("entry", "deployed_at", block.number)
  4. Wrap in array: string.concat("[", entry, "]") and vm.writeJson(json, "../meshes.json")
  5. console.log summary: contract address, mesh name, block number, chain ID
- Key: "../meshes.json" resolves to repo root since Foundry project root is agentmesh-contracts/
- startBroadcast uses --private-key or --account from CLI invocation
      </action>
      <refactor>
REFACTOR — after tests pass:
1. Extract the JSON path into a constant string at contract level for easy override in tests.
2. Add natspec @notice on run() documenting MESH_NAME env var requirement.
3. Confirm the relative path resolves correctly by running forge script in dry-run mode and checking meshes.json appears at repo root; adjust path string if needed.
      </refactor>
      <verify>
cd agentmesh-contracts
MESH_NAME=TestDeploy forge test --match-contract DeployScript -vv
MESH_NAME=AgentMesh-Sepolia forge script script/Deploy.s.sol --rpc-url https://rpc.sepolia.org -vvvv
cat ../meshes.json
      </verify>
      <done>
- agentmesh-contracts/script/Deploy.s.sol compiles with forge build (0 warnings, 0 errors).
- All 3 DeployScript tests pass: forge test --match-contract DeployScript exits 0.
- forge script dry-run completes without revert and prints summary to stdout.
- meshes.json exists at repo root containing a single-element JSON array with address, name, deployed_at keys.
- Existing 23 AgentRegistry tests still pass.
      </done>
    </task>

    <task type="auto">
      <n>Validate deploy path and add .gitignore entry for meshes.json</n>
      <context>vm.writeJson resolves its path argument relative to the Foundry project root (the directory containing foundry.toml), not the shell CWD. The project root is agentmesh-contracts/. Therefore "../meshes.json" would write to the repo root. meshes.json is a generated deployment artifact and should not be committed to git by default.</context>
      <domain>Project configuration, deploy artifact management</domain>
      <files>agentmesh-contracts/.gitignore or repo root .gitignore — add meshes.json entry; agentmesh-contracts/script/Deploy.s.sol — confirm/fix JSON output path</files>
      <test>
RED — shell-level integration check:
cd agentmesh-contracts
MESH_NAME=PathTest forge script script/Deploy.s.sol --rpc-url https://rpc.sepolia.org
[ -f ../meshes.json ] and echo "PASS: repo root" or echo "FAIL: not at repo root"
[ ! -f meshes.json ] and echo "PASS: not in contracts dir" or echo "FAIL: wrong location"
      </test>
      <action>
GREEN:
1. Run the path-check above. If "../meshes.json" lands at repo root, keep as-is. If it lands inside agentmesh-contracts/, adjust the path in Deploy.s.sol.
2. Add to .gitignore: meshes.json
3. Add a comment in Deploy.s.sol: // vm.writeJson paths are relative to foundry project root (agentmesh-contracts/)
      </action>
      <refactor>
Add a comment block at the top of Deploy.s.sol documenting exact invocation commands:
// Deploy to Sepolia: MESH_NAME=AgentMesh-Demo forge script script/Deploy.s.sol --rpc-url $RPC_URL --account deployer --broadcast
// Verify: forge verify-contract <address> src/AgentRegistry.sol:AgentRegistry --chain sepolia --etherscan-api-key $ETHERSCAN_API_KEY
      </refactor>
      <verify>
MESH_NAME=PathTest forge script script/Deploy.s.sol --rpc-url https://rpc.sepolia.org
ls -la ../meshes.json
grep "meshes.json" ../.gitignore || grep "meshes.json" .gitignore
      </verify>
      <done>
- meshes.json is written to the repo root after a forge script dry-run.
- meshes.json appears in .gitignore so git status shows it as ignored.
- Deploy.s.sol has a comment block documenting live broadcast and verify commands.
- All existing tests continue to pass.
      </done>
    </task>
  </tasks>
</plan>