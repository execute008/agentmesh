---
phase: 3
plan: 1
depends_on: []
must_haves:
  - "forge test exits 0 with all tests passing (23 total)"
  - "forge coverage shows branches ≥90%, lines ≥95%, functions 100%"
  - "Reputation cap at 100 is tested (11 sequential task completions)"
  - "Transfer failure revert is tested (RejectEther helper contract)"
  - "Scaffold.t.sol placeholder is deleted"
---

<task type="auto">
  <n>Add reputation cap test (branch coverage)</n>
  <context>Escrow & Settlement / Reputation</context>
  <domain>Agent (reputation field), Task (escrow lifecycle)</domain>
  <files>agentmesh-contracts/test/AgentRegistryTest.t.sol</files>
  <test>
    RED phase: Write a failing test that proves reputation caps at 100.
    - test_releasePayment_capsReputationAt100: Register bob (rep=50), create and complete 11 tasks
      sequentially (each releasePayment adds +5). After 10 releases rep should be 100 (50+50).
      After 11th release rep should still be 100, not 105.
    - Assert: registry.getAgent(bob).reputation == 100 after 11 releases
    - This test currently does not exist, so it's "red" by absence.
  </test>
  <action>
    GREEN phase: Add test_releasePayment_capsReputationAt100 to AgentRegistryTest.
    - Use a loop: for taskId 1..11, hoax alice with ETH, createTask, prank bob completeTask, prank alice releasePayment.
    - After task 10: assertEq(registry.getAgent(bob).reputation, 100)
    - After task 11: assertEq(registry.getAgent(bob).reputation, 100) — cap holds
    - Run forge test — should pass immediately since contract logic already handles the cap.
  </action>
  <refactor>
    No refactoring needed — this is a pure test addition.
  </refactor>
  <verify>forge test --match-test test_releasePayment_capsReputationAt100 passes</verify>
  <done>test_releasePayment_capsReputationAt100 passes, branch for `if (newRep > 100)` now covered</done>
</task>

<task type="auto">
  <n>Add transfer failure test (branch coverage)</n>
  <context>Escrow & Settlement</context>
  <domain>Task (releasePayment ETH transfer), Agent (malicious executor)</domain>
  <files>agentmesh-contracts/test/AgentRegistryTest.t.sol</files>
  <test>
    RED phase: Write a test that triggers the "Transfer failed" revert branch.
    - test_releasePayment_revertsIfTransferFails: Deploy a helper contract (RejectEther)
      that has a receive() function that reverts. Register this contract as an agent.
      Create task with this contract as executor. Complete task. releasePayment should revert
      with "Transfer failed" because the executor contract rejects ETH.
    - This branch (line 167-168) is currently uncovered.
  </test>
  <action>
    GREEN phase: Add RejectEther helper contract and test_releasePayment_revertsIfTransferFails.
    - Define `contract RejectEther { receive() external payable { revert(); } }`
      as a helper at the bottom of the test file.
    - In the test: deploy RejectEther, prank as RejectEther address to registerAgent,
      alice creates task with RejectEther as executor, prank RejectEther to completeTask,
      alice calls releasePayment → expect revert "Transfer failed".
    - Run forge test — should pass.
  </action>
  <refactor>
    No refactoring needed — this is a pure test addition.
  </refactor>
  <verify>forge test --match-test test_releasePayment_revertsIfTransferFails passes</verify>
  <done>test_releasePayment_revertsIfTransferFails passes, "Transfer failed" branch now covered</done>
</task>

<task type="auto">
  <n>Remove scaffold placeholder test</n>
  <context>Registry (test infrastructure)</context>
  <domain>N/A — cleanup</domain>
  <files>agentmesh-contracts/test/Scaffold.t.sol</files>
  <test>
    RED phase: Scaffold.t.sol exists with a meaningless assertTrue(true) test.
    It should be removed as it adds no value and was a Phase 1.1 placeholder.
  </test>
  <action>
    GREEN phase: Delete agentmesh-contracts/test/Scaffold.t.sol.
    Run forge test to confirm all remaining tests still pass.
  </action>
  <refactor>
    No refactoring needed.
  </refactor>
  <verify>forge test passes with 23 tests (21 existing + 2 new), Scaffold.t.sol gone</verify>
  <done>Scaffold.t.sol deleted, forge test reports only AgentRegistryTest suite</done>
</task>

<task type="checkpoint:human-verify">
  <n>Verify coverage meets threshold</n>
  <context>All contexts in AgentRegistry</context>
  <domain>AgentRegistry aggregate — all paths</domain>
  <files>agentmesh-contracts/test/AgentRegistryTest.t.sol</files>
  <test>
    Run forge coverage and verify:
    - Lines: ≥95%
    - Statements: ≥95%
    - Branches: ≥90%
    - Functions: 100%
  </test>
  <action>
    Run: cd agentmesh-contracts && forge coverage
    Review output table. All critical paths (registration, escrow, reputation, search) should show full coverage.
  </action>
  <refactor>
    If any metric is below threshold, identify the uncovered lines and add targeted tests.
  </refactor>
  <verify>forge coverage output shows all metrics above thresholds</verify>
  <done>Coverage report shows Lines ≥95%, Branches ≥90%, Functions 100%. Phase 1.3 exit criteria met.</done>
</task>